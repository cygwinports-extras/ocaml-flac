inherit ocaml

DESCRIPTION="OCaml bindings for the FLAC audio library"
HOMEPAGE="http://savonet.sourceforge.net/"
SRC_URI="mirror://sourceforge/savonet/${P}.tar.gz"
PATCH_URI="0.1.0-vpath.patch
           0.1.0-byteswap.patch"

src_compile() {
	lndirs
	cd ${B}
	cygconf
	cygmake -j1
}
